# AGENTS.md — dsh-inbox 项目规范

> 本文件是项目规范的**单一源**。`CLAUDE.md` 只有一行 `@AGENTS.md` 引用式跟随，不手动同步。

## 项目概览

- **一句话**：dsh-inbox 是 DeepSeek Harness（dsh）的插件，把日常复制粘贴的链接、图片、文本、账密收进一个本地仓库，自动分类、可在侧栏浏览、可在 dsh 对话里检索取回。
- **面向谁**：公开 npm 包 `@duoyu/dsh-inbox`，任何人都能像装社区插件一样安装。
- **当前阶段**：M0–M6 已验收；**M7（界面升级）进行中**，M8（打包与一键安装）未开始。阶段与验收标准见 `docs/feature/dev-bus.md`。
- **平台**：代码跨平台；**v1 只在 Windows 实测验收**，macOS/Linux 未验证（README 需如实写明）。
- **包名**：`@duoyu/dsh-inbox`。

## 技术栈

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | TypeScript（ESM） | dsh 插件生态统一形态 |
| Node | ≥ 22 | 用内置 `node:sqlite` 做仓库存储 |
| 包管理 | pnpm | 与 dsh 一致（`dsh plugin` 转发 pnpm） |
| 打包 | tsdown | 官方 client 包全部用 tsdown 产出 `lib/client.js` |
| 测试 | vitest | 官方前端包同款 |
| 客户端 UI | React 18 + dsh 官方 slot 机制 | 不自造 UI 框架 |
| 宿主侧 | Cordis 插件（`@deepseek-ai/cordis`） | dsh 的插件运行时 |

## 目录结构

```
src/
  host/        宿主侧：工具注册、仓库存储、分类、WebDAV 摄取
  client/      浏览器侧：侧栏面板、对话卡片视图
  shared/      两侧共用的类型与常量
docs/
  help/        知识文档（单点主题）+ index.md
  feature/     特性开发文档（dev-bus.md、阶段验收记录）
  handoff/     交接文档
  prototype/   界面原型（*.prototype.html，一次性，双击预览）
.research/     官方 dsh 分发的临时参考副本（已 gitignore，用完删除）
```

## 关键约束与约定

1. **单包双半边**：`package.json` 同时声明宿主侧 `dsh.bundle.patch` 与浏览器侧 `dsh.client`（`platform: "web"` + 导出 `./client`）。客户端 bundle 由 tsdown 生成，**禁止手写 `window.__ModuleLoader__.load(...)`**。
2. **工具必须挂进 agent preset**：`defineTool` 注册的工具只有在会话的 `agent.cordis.yml` 里挂上才对模型可见。安装体验必须自动化这一步（`init` 命令），不指望用户手工改 preset。
3. **密钥红线**：账密类资源**加密落盘（2026-09-20 已实现，域 v6）**——正文进 `secret`（AES-256-GCM，密钥由主密码 scrypt 派生），主密码与派生密钥**都不落盘**（只在进程内存里，重启即锁，界面显式解锁）；新捕获的账密在**没有密钥时直接拒绝入库**（绝不退回明文），老明文记录在首次设置/解锁时自动转密文。列表脱敏、明文**永不进模型上下文**、对话里**永不输出明文**，长期保留。任何发往模型的分类请求先过脱敏。列表里密钥/账密记录**只显示你起的名字**，没起名字时才用你的备注，**绝不显示正文**；取名规则只写在一处（`src/client/heading.ts`，列表与 dock 共用），「这是什么」由类目图标（钥匙）表达。**由此有一条不变式：`title` 只能由用户在面板里填**——规则、模型、抓来的页面标题等任何自动路径都不许往 `title` 写（那正是"列表把密文当标题"这条老漏洞的成因；抓来的标题进 `linkTitle`，`model.ts` 只 patch `category`/`categorySource`）。
4. **图片与证件**：分类时**允许把图片发给模型判断**（2026-09-19 用户授权——手机拍的证件照比例就是普通照片，靠宽高判不出来）；对话里默认只回 `[attachment:<id>]` 标记，字节不进对话。**唯一的例外**（2026-09-20 用户授权）：`inbox_get` 显式带 `withImage: true` 时，把该记录的那张图作为 content part 发给模型——用于"帮我看这张图是什么"，**每次调用都要显式要**、默认永不发。类目结论里，只有**用户亲口说的**才算确定证件；模型判的标 `categorySource: model`，可以随时被用户改掉。模型调用受预算闸门约束（见 `docs/help/…` 与 `src/host/classify/model.ts`）。
5. **上下文红线**：不自动把 inbox 摘要注入模型上下文；模型只有主动调工具时才看到仓库内容。
6. **用户描述 = 权威分类**：用户给的描述/类目优先于任何模型判断，模型不得覆盖。
7. **同步红线（2026-09-20 用户拍板改）**：**上云的密文只有账密正文**——账密的 `secret` 本来就是密文，别的字段（文本、链接、标题、备注、类目、标签、时间）和附件**字节**都按明文上云。因此 **远端必须有访问控制**（README 写明"别用公开桶"），并且：主密码与派生密钥永不上传、永不同步；冲突**直接按 `updatedAt` 覆盖**（不留冲突副本）；删除以墓碑同步，清空回收站连远端一起删；附件随同步走。
8. **数据事实只认实测**：dsh 处于 rc 阶段（本机 0.1.5-rc.2），API 会变。写 dsh 相关代码前先查 `docs/help/dsh-plugin-platform.md`；该文档与代码冲突时以 `.research/` 里的官方源码为准，并回改文档。
9. **文档语言**：代码与标识符英文，代码注释中文；文档中英双份（`README.md` + `README.zh.md`），`AGENTS.md`/`docs/` 用中文。
10. **持久化只走 `ctx.storageDomain`**：不自己开文件、SQLite 或别的存储；域 spec 的 schema 演进见 `docs/help/vault-data-model.md`。
11. **客户端半边不许引入宿主依赖**：`src/client/**` 只能 import React、平台静态模块表里的包，以及 `src/shared/` 下的纯常量（不许 zod、不许 `node:*`、不许 `@deepseek-ai/dsh-*`），否则浏览器产物会在加载期炸。
12. **面板跟随宿主主题，别写死**：配色方案从宿主读（`<html>` 的 `color-scheme`，读不到再按继承来的文字色亮度判），实现在 `src/client/scheme.ts`，判定与三个坑见 `docs/help/panel-theme.md`。**禁止**再出现硬编码的 `color-scheme`，也别读面板自己的（那是自我回声）。

## 常用命令

| 用途 | 命令 |
|---|---|
| 安装依赖 | `pnpm install` |
| 构建 | `pnpm build`（esbuild → `lib/index.js` + `lib/client.js`） |
| 类型检查 | `pnpm typecheck` |
| 测试 | `pnpm test`（vitest） |
| 起隔离开发 profile | `dsh --profile inbox --no-open --port 3102` |
| 挂载本仓库到 profile | `dsh plugin --profile inbox add <仓库路径>` |
| 验证模型能调到工具 | `dsh --profile inbox-m0 "<让模型调用工具的提示>"`（headless 派生 profile） |

本机 dsh 不在 PATH 上，完整命令（含 Node 22 路径）见 `docs/help/dev-setup.md`。构建需要提权（esbuild 要 spawn 子进程）。

## 知识文档索引

| 主题 | 路径 |
|---|---|
| dsh 插件平台实测事实（扩展点、限制、证据） | `docs/help/dsh-plugin-platform.md` |
| 已锁定的产品与架构决策 | `docs/help/product-decisions.md` |
| 对象存储网关兼容性排查（401/403 的三种可能、客户端标识） | `docs/help/remote-gateway-compat.md` |
| 开发总线（阶段 / 验收标准 / 状态） | `docs/feature/dev-bus.md` |
| 知识文档总索引 + 维护记录 | `docs/help/index.md` |

## 通用宪章块

### 文档分工

| 文档 | 读者 | 内容 | 变化触发 |
|---|---|---|---|
| `README.md` | 人（开发者/使用者） | 是什么、功能特性、快速开始、常用命令 | 用户可见命令/用法/项目定位变化 |
| `AGENTS.md` | agent | 代码组织、规范、约束（单一源，CLAUDE.md 引用式跟随） | agent 写码规则变化 |
| `docs/help/{topic}.md` | agent | 关键逻辑知识（下次还会用到、不看代码猜不出） | 可复用知识增量（新模块/契约/排查方法/流程/关键决策） |
| `docs/help/index.md` | agent | 主题索引 + 末尾「维护记录」表（维护锚点，**最多保留 5 条**） | 任何 help 文档增删改 |

### 文档目录约定

- `docs/help/` — 知识文档（单点主题 `{topic}.md` + 索引 `index.md`），AGENTS.md 只记「主题 → 路径」，按需读取
- `docs/feature/` — 特性开发文档（开发总线、阶段验收记录）
- `docs/handoff/` — 交接文档（`{YYYY-MM-DD}-{topic}.md`，由 handoff 流程生成）
- `docs/prototype/` — 界面原型（`*.prototype.html`，文件名带日期，本地双击预览；一次性代码，不是产品的一部分）
- `docs/assets/` — 仓库里要展示的图片（README 的封面与截图）；截图建议窗口宽 ≥ 1200px，否则三栏会塌成一栏

### 通用工作方式（独立判断基线）

- 执行前检查错误前提、逻辑跳跃和缺失信息；不盲从用户描述与既有注释，以代码为准
- 独立判断，区分事实、预测和观点；涉及数字、人物和结论时核实信息来源
- 发现用户说得不对时直接指出，并说明依据、风险和更合适的解释
- 主动提醒可能忽略的变量、成本和偏差
- 按工作场景补充守门维度：测试、引用、权限、上线检查等

### 知识维护步骤（任务收尾、向用户汇报前强制执行）

1. **判断增量**：任务完成后有可复用知识（新模块/新规范/新契约/新排查方法/新流程/关键决策）→ 维护；纯 bug 修复/配置调整/无行为变化的重构 → **不为维护而维护**，汇报时说明"无增量"
2. **写/改文档**：以代码为准；有现成 topic → 扩展，无 → 新增 `docs/help/{topic}.md`（单点主题，无 frontmatter）
3. **同步索引**：新增/改名/归档文档必须更新 `docs/help/index.md` 条目
4. **联动根文档**：**每个模块收尾都要核对并更新 README 的「安装 / 卸载 / 开发 / 当前可用功能」四节**（命令、路径、依赖、限制一旦变化就必须同步，中英两份一起改）；规范/关键链接变化 → AGENTS.md（CLAUDE.md 不手动改）
5. **提交（白名单）**：只提交 `docs/help/**`、`README.md`、`AGENTS.md`、`CLAUDE.md`，绝不带源码/依赖文件；有无关未提交变更时只 add 白名单文件，不 `git add -A`

### 维护锚点（跨 session 补盲区）

- **锚点**：`docs/help/index.md` 末尾「维护记录」表记录上次维护时的 HEAD（**最多保留 5 条**，超出删除最旧，避免索引过长）
- **圈变更**：`git log {上次HEAD}..HEAD --name-only`（含他人提交）+ `git status --short`（未提交 diff），合并后判断增量
- **固化**：先提交文档 → `git rev-parse --short HEAD` 拿 commit id → 回填维护记录表（维护时间 + 维护时 HEAD + 变更摘要）→ 再提交一次固化
- 锚点必须是**已提交的 HEAD**，否则下次 `git log {anchor}..HEAD` 会把本次维护的文档提交也圈进来

### 回复参考声明

任务执行类回复（改代码/排查/维护文档/提交等）在回复**最末尾**声明**本轮**对 docs 知识文档的参考情况（docs/help/index.md 索引覆盖的文档）：

- "参考"= **本轮内实际读取过**文档（Read / WebFetch 等主动读取）；**编辑/修改文档不算阅读**——编辑发生在哪一轮，就在哪一轮声明
- 读了 → 列清单：文档名 + 反馈标记 + 用途一句话
- 本轮未读任何 docs 文档（含跨轮延续：仅提交/复用上轮已读或已编辑的内容）→ 声明「本次未参考 docs 文档」（暴露文档盲区），**不要凭上轮记忆打 ✓**
- 纯闲聊 → 不声明

反馈标记：
- `✓` 有用（不落盘）
- `⚠️` 过时/不匹配 → 顺手在 docs/help/index.md 对应条目后追加 `⚠️ {YYYY-MM-DD}: {问题一句话}`，由维护流程读取清理
- `未找到` 主题缺失 → 同上落盘

位置与格式：置于整个回复最末尾（验证证据/目录树之后），用 `---` 分隔线独立成块，不套用表情规则。

### 上下文预算原则

- **AGENTS.md ≤ 200 行软预算**：超出时把细节外移 docs/help，AGENTS.md 只留索引（不硬切，但维护步骤与 audit 会持续提示）
- 上下文有效性 ≈ 相关信息密度 × 当前性 × 可操作性 ÷ 冲突与噪声
- 单条规则超过 3 行说明 → 详情进 docs/help，AGENTS.md 写一句话规则 + 索引

### 验证与提交守门（三层，提交前执行）

**层 1 — 机器验证（必须）**：源文件编码 UTF-8 无 BOM（禁止非 UTF-8 工具写文件，曾致中文乱码/编译失败）；逻辑改动（工具库/纯函数等）补充/更新对应单测；提交前单测基线全绿；运行守门检查（`pnpm typecheck && pnpm test`）全部通过后才能提交，开发时可运行监听模式（`pnpm test --watch`）自动重跑。

**层 2 — 独立审查（必须）**：提交前把 diff 当"别人写的"从头审一遍——前提/需求语义是否一致、逻辑跳跃与边界漏判、数字/金额/结论处核对来源、安全与权限（密钥、越权、日志泄露）、命名与抽象一致性。发现问题 → 给修复清单，修完复审，不静默通过。重大/高风险变更派独立子代理审查（生成与判定分离）。

**层 3 — 人工确认（按需）**：不可逆操作、破坏性重构、对外行为变更（npm 发布、profile 装配脚本、改 preset 默认值）提交前向用户确认。

### 多 agent 协作约定

- **并行适用**：独立探索（互不依赖的调研）、独立生成（文件互不重叠）、独立验证（review/测试与实现并行）可并行；共享可变状态的写、同一文件并发编辑、依赖前序结论的决策**不可**并行
- **任务说明四要素**：给子代理的说明必含——目标 + 边界（做什么不做什么）+ 输入引用（具体文件路径）+ 输出格式（结构化：结论 + 证据 `文件:行号` + 待确认项）
- **结果合并与仲裁**：合并责任在主 agent；冲突由主 agent 仲裁，争议上报用户，不静默取其一
- **生成与判定分离**：实现者不判自己的产出；独立验证优先派不同 agent/视角
- **上下文预算延伸**：每个子代理只喂它需要的上下文（相关文件 + 相关规范段落），不整份复制 AGENTS.md
- **越权防护**：只读调研只回文字，禁止创建/复制文件、**禁止启动 dsh 服务、禁止构建**（会污染工作树、占用端口、留下未审查的改动）；确实需要落地时，任务说明里显式写清可写范围
- **只读任务不要 fork 全历史**：`fork_turns` 用 `none` 或少量，并在说明**第一行**点明「你是子代理，主 agent 另有其人，只做审查/调研」。全历史 fork 的子代理会继承主 agent 的目标，可能把自己当主 agent 把活干完并提交（2026-09-20 M7.16 实测：一次只读审查改文件 + 连提三个 commit）

### 安全与敏感信息

- 密钥/Token/凭据禁止写入代码、文档与提交；配置走环境变量/密钥管理
- 禁止在日志、报错、回复中输出敏感信息（手机号、身份证、密钥等）
- 新依赖引入前确认来源可信（版本锁定，警惕同形包/投毒）
- **同源路由只发白名单类型**：附件路由与面板同源，因此可服务的媒体类型是白名单（`INBOX_IMAGE_TYPES` 或 `video/`、`audio/`）+ `nosniff`；**绝不按客户端声明的类型发任意 content-type**（`image/svg+xml`、`text/html` 这类同源可执行）
- **对外请求只走官方 seam**：抓链接标题（`src/host/link-title.ts`）是唯一的"插件自己发起的对外请求"，它一次只 GET 一个 URL，必须走 `ctx.web`（`@deepseek-ai/dsh-web`：解析并固定公网地址、拒私网、只跟同源跳转、限时限量、不带 cookie）；**只在本机捕获的 `kind=link` 上触发**，远端同步拉进来的链接不抓。改这里前先读第 4 条和第 3 条：一次外发请求等于告诉对方"这台机器打开过这个链接"
- **抓不到 ≠ 站点不给**（2026-09-20 踩过）：微信按 `User-Agent` 认客户端，harness 默认自称 `deepseek-harness/…`，于是只回一个 `<title>` 为空的空壳页。UA **不是每次请求的参数**（`WebFetchRequest` 只有 `url`），它属于 profile 里 `web-fetch-http` 的配置——要改只能改 profile 的 `cordis.patch.yml`（已经这么做了，配置里带注释说明它影响该 profile 的全部抓取，含模型的 web 工具）。下一次遇到"抓不到"，先确认我们发了什么头、再下结论
- **日志只记 host**：抓取类诊断日志打 `new URL(url).host`，**不打整条 URL**——query 里可能带着 token；失败原因要能被用户看见就写进记录（`linkTitleError` 这类字段），别指望日志（`ctx.logger.info` 默认不落 stdout，实测过）
- 权限/越权/资金/对外接口相关改动，提交前按层 2 重点审查

### 与 agent 自带记忆的分工

- AGENTS.md/docs/help 存**跨 session 稳定规则**（规范、约束、索引）；agent 自带记忆存**会话级事实**（任务上下文、用户偏好）
- 规则进 AGENTS.md 不写进记忆；会话级事实不写进 AGENTS.md

### 变更粒度（大改动渐进验证）

- 大改动拆成小步提交，每步可独立验证（编译/单测通过）后再走下一步；禁止一次性提交"半成品大 diff"
- 重构保持行为不变（先重构、后改行为，两步分开验证）
